name: VotingSample
services:
- name: vote
  project: vote.csproj
  dependsOn:
    - redis
- name: worker
  include: ../worker/tye.yaml
- name: redis
  image: redis
  bindings:
    - port: 6379
      connectionString: ${host}:${port}
